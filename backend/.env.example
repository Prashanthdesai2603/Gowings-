# Database Configuration (Provide individual variables or the full URI)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

# Application Configuration
PORT=5000
JWT_SECRET=your_jwt_secret_here

# Render Deployment
# Ensure you configure DATABASE_URL or DB_* variables in the Render Dashboard
